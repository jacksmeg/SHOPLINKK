services:
  - type: web
    name: shoplinkk-web
    runtime: node
    plan: starter
    region: frankfurt
    buildCommand: npm ci --include=dev && npm run db:generate && npm run build
    preDeployCommand: npm run db:deploy && npm run db:seed
    startCommand: npm run start
    healthCheckPath: /api/health/ready
    domains:
      - www.shoplinkk.com
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: shoplinkk-postgres
          property: connectionString
      - key: NEXTAUTH_URL
        value: https://www.shoplinkk.com
      - key: NEXT_PUBLIC_APP_URL
        value: https://www.shoplinkk.com
      - key: NEXTAUTH_SECRET
        generateValue: true
      - key: INTEGRATION_ENCRYPTION_KEY
        generateValue: true
      - key: BACKUP_RETENTION_DAYS
        value: "14"
      - key: SHOPLINKK_ADMIN_EMAIL
        sync: false
      - key: SHOPLINKK_ADMIN_PASSWORD
        sync: false
      - key: ADMIN_ALERT_EMAIL
        sync: false
      - key: LOG_WEBHOOK_URL
        sync: false

databases:
  - name: shoplinkk-postgres
    plan: basic-256mb
    region: frankfurt
